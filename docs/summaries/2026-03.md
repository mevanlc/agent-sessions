## March 2026

- Codex usage tracking: Visible limits refresh now prefers auth-backed data first, falls back to CLI RPC and JSONL when needed, and only uses `/status` as a last resort. The Preferences copy now matches that source order.
- Codex usage tracking: `/status` fallback snapshots now refresh per-window limits correctly again, menu-background refreshes still use OAuth/CLI before probing, and partial live updates no longer leave the other window stuck on stale data.
- Launch/TCC: Startup session rows now resolve project names without implicit git-root filesystem probing, reducing spurious Photos/Music permission prompts after rebuild/cold launch.
- Launch/TCC: Preferences agent status rows and OpenCode backend detection now fall back to stored non-probing state when loaded by build tooling, reducing stray filesystem checks during rebuild-time metadata extraction.
- Launch restore: Saved Sessions-only relaunch now also installs shared window-open routing callbacks, preventing menu-bar open actions from no-oping in that restore path.
- Agent Cockpit HUD: Codex runtime-backed subagent badges now resolve parent sessions correctly when the selected log is a child rollout, and runtime DB lookup now follows `CODEX_HOME`/`SessionsRootOverride` before defaulting to `~/.codex`.
- Agent Cockpit HUD: Codex active subagent badges now read the runtime `thread_spawn` state DB when available, so open worker agents remain counted even while their rollout files are idle; passive `lsof`/rollout heuristics are now only a fallback.
- Usage display: Codex now stores auto-probe cooldown separately from UI freshness, so Cockpit and in-app usage labels no longer look freshly updated for hours after a `/status` probe.
- Usage (Codex): Recent rollout logs that omit `rate_limits` no longer cause the app to reuse older session-file limits as if they were current. Usage surfaces now mark those limits unavailable in recent logs and let the probe fallback recover current values when possible.
- Agent Cockpit: The pinned limits footer now refreshes from the same live Codex/Claude usage-model updates as the main window footer, avoiding stale percentages that previously lingered in Cockpit after the shared usage snapshot changed.
- Agent Cockpit: Hover-expanded weekly reset labels now use the same `Day 1:34 PM` format for Codex and Claude.
- Agent Cockpit: Cockpit-only and pinned-only launches now follow the same shared startup bootstrap as the unified window, fixing Claude rows that previously stayed generic until the main window opened.
- OpenCode / Agent Cockpit: `3.2` adds OpenCode live-session support to Agent Cockpit and shared menu bar active/waiting summaries, so Codex, Claude, and OpenCode now surface through the same live-status UI.
- OpenCode: Agent Sessions now reads OpenCode v1.2+ SQLite storage automatically, falls back to legacy JSON session files when needed, and exposes the detected backend in Preferences.
- OpenCode: Follow-up `3.2` fixes restore main-list live dots for OpenCode sessions, improve iTerm busy-state detection, and harden SQLite search hydration/direct session-root overrides.
- Agent Cockpit / Menu Bar: OpenCode live sessions now feed the shared active/waiting summaries and iTerm presence detection, keeping cockpit and menu bar counts aligned across Codex, Claude, and OpenCode.
- Onboarding: Fixed stale session counters in onboarding by refreshing cached visible/hidden counts whenever live session data changes, even when total session counts stay the same.
- Onboarding/Index snapshot: DB fallback now computes visible counts by applying the active onboarding filter logic to hydrated session metadata, keeping startup counts aligned with current filter preferences.
- Agent Cockpit: Major stability update. Fixed the Cockpit CPU/energy leak, eliminating long-run resource creep while pinned or backgrounded, with reduced row flicker, better missing-session fallback, and less probe churn over time.
- Window restoration: Relaunch now reliably reopens the `Agent Sessions` main window (including prior frame/position), and also auto-opens `Agent Cockpit` when it was pinned.
- Agent Cockpit: Relaunch/background behavior is more reliable, including correct `Open Agent Sessions` routing and tooltip layering above pinned HUD windows.
- Menu Bar: Added `Show Active/Waiting sessions` plus a `Show menu bar icons` setting so live session dots can be controlled separately from usage meters.
- Preferences: Added an `Advanced` toggle to hide the Dock icon; the app stays in its normal Dock-visible mode by default.
- Preferences: `Hide Dock icon` now auto-enables the menu bar item to preserve a reopen path, and policy falls back to Dock-visible mode when no persistent app entry point exists.
- Menu Bar: Fixed `Open Agent Cockpit` routing to target the cockpit window identifier directly, avoiding no-op matches against unrelated untitled windows.
- Agent Cockpit: Non-active live sessions now use `Waiting` terminology, compact pinned mode keeps its toolbar visible, grouped headers stay single-line, and stale 4h+ waiting work is visually deprioritized.
- Menu Bar / Agent Cockpit: Live counts and quick actions are surfaced more clearly, with fixes for hidden menu bar items and unresolved Codex live presences.
- Docs/Website: README and GitHub Pages now include a dedicated Agent Cockpit showcase box with light/dark cockpit screenshots for the 3.0 release narrative.
- Agent Cockpit: Active green live-status dots now use a strictly static render path (no pulse), matching the main Sessions list; idle amber dots remain pulsing.
- Agent Cockpit: Compact mode now defaults to stable fixed-height sizing (`Medium`, 4 rows) with internal scrolling instead of continuous row-count auto-resize, preserves manual compact resizing across full↔compact toggles and app restarts, and exposes `Small/Medium/Large` size controls in Settings plus an optional `Auto-fit compact height to visible sessions` toggle.
- Onboarding (v3): New users now see `Agent Cockpit (Beta)` as slide 3 (after `Connect Your Agents`) so cockpit workflows are introduced before session/analytics deep-dives.
- Onboarding (v3 updates): Upgrade onboarding is now a two-slide sequence focused on Agent Cockpit usage plus a final feedback/community-support slide with Google Form, GitHub Sponsors, and Buy Me a Coffee links.
- Agent Cockpit: Removed visible row numbers from HUD rows, kept keyboard shortcuts bound to current visual order (`Cmd+1...9`), and added one-shot highlight washes for newly inserted rows.
- Agent Cockpit: Full-mode subtitles now correctly prefer custom iTerm window titles when iTerm session naming is set to default job labels (for example `codex`), restoring missing subtitle text for configured window labels.
- Agent Cockpit: Pin/unpin now restores standard non-topmost window behavior reliably when unpinned.
- Agent Cockpit: Tab subtitles now show only custom tab/window names (for example `CC - AS III` from `CC - AS III (codex*)`), full-mode Agent↔Session spacing was tightened, and idle title text remains readable in both compact and full modes.
- Agent Cockpit: Compact and full HUD rows now use the same status-dot renderer as the main Sessions list, keeping active green dots steady (non-pulsing) in cockpit.
- Agent Cockpit: Active green status dots now ignore parent row animations so they remain visually static in cockpit, matching the main Sessions list.
- Agent Cockpit: Compact mode now shows a dedicated tab/window subtitle column after `Agent`, defaults to a narrower first-run compact width (showing shorter session-name snippets), auto-hides compact header controls while unfocused until mouse hover, shrinks/expands compact window height with toolbar visibility, keeps top/bottom spacing visually balanced for short unfocused lists, and auto-fits focused compact height to visible sessions (up to 10 rows before scroll).
- Agent Cockpit: Grouped compact mode now preserves manual fit on focus-loss transitions by hiding only toolbar height (without extra row shrink), preventing clipped bottom project/session rows.
- Agent Cockpit: Compact/full mode switches now keep user-resized window sizes in both directions, unpinned-but-visible cockpit windows now keep background live probing warm to avoid intermittent `No active sessions` flicker when unfocused, and grouped `Idle` pills now use the same amber palette as idle dots.
- Agent Cockpit: Ordering now stays locked while the cockpit window is visible, only re-sorting after hide/minimize transitions; compact/full window sizes are now persisted independently (full default 644x320, compact default 6 rows with 3-row minimum) and no longer auto-resize when sessions are added or removed.
- Agent Cockpit: Fixed compact-to-full transitions to reliably restore standard macOS window chrome (title bar plus red/yellow/green controls).
- Agent Cockpit: Full mode now consistently preserves iTerm subtitle text under agent names, with truncation behavior unchanged.
- Agent Cockpit: Added an Agent Cockpit toolbar button to the main window for faster access.
- Agent Cockpit: Added row context menu actions (`Go to Session`, `Focus in iTerm2`, `Reveal Log`, `Open Working Directory`, and copy helpers); `Go to Session` now auto-reveals hidden sessions by relaxing restrictive filters when needed.
- Agent Cockpit: Full and compact modes now both surface scroll indicators when lists overflow.
- Agent Cockpit + Session list (Unified): `Go to Session` now survives main-window initialization races via pending handoff retries and clears `Saved Only` filtering during auto-reveal so non-favorite targets can still be selected.
- Agent Cockpit + Session list (Unified): Updated live status hierarchy and contrast polish: louder pulsing idle amber, calm static active green, 7pt status dots, improved badge saturation/text contrast/time contrast, group-header idle pill styling, and idle-row opacity raised to 0.60.
- Agent Cockpit: Removed the legacy cockpit runtime window path so only `Agent Cockpit` opens in-app.
- Agent Cockpit: Full-mode recency now uses last write activity (not heartbeat), compact mode hides the recency token, and full-mode rows optionally show iTerm tab subtitle under the agent label with truncation + hover tooltip for long titles.
- Agent Cockpit: iTerm subtitle labeling now falls back to window title when a tab title is empty, so full-mode subtitle context remains visible.
- Agent Cockpit: Full-mode subtitle updates now track current iTerm tab titles even after launch-time tab-name edits, match by iTerm session GUID when needed, keep subtitle discovery resilient when iTerm blocks tab-title variables for individual sessions, and avoid refresh cadence stalls by replacing per-session tab-title probes with a single guarded iTerm scan.
- Agent Cockpit: Full-mode subtitle extraction now uses iTerm’s native tab `title` property, restoring subtitle visibility for tabs that define only tab title while leaving window title empty.
- Agent Cockpit: Idle HUD rows now keep the session display name visible, move activity recency into a compact elapsed column (for example `16s`), show active dots as solid green, and pulse idle amber dots with a larger size after 10 minutes idle.
- Session list (Unified): `CLI Agent` live-state visuals now match Agent Cockpit, with solid green active dots, pulsing amber idle dots that grow slightly after 10 minutes idle, and dimmed idle source cells.
- Agent Cockpit + Session list (Unified): Idle amber status dots now brighten at pulse peak, improving visibility of the pulsing animation.
- Agent Cockpit: Follow-up CPU pass now avoids publishing redundant HUD snapshot timestamps, so unchanged live snapshots no longer trigger repeated full presentation rebuild cascades.
- Agent Cockpit + Codex live polling: Idle pulse animations now pause while the app is inactive, and pinned/background stable-state polling now backs off from `3s` to `5s` after repeated unchanged cycles to reduce long-run CPU drift while preserving near-instant foreground updates.
- Agent Cockpit: Active/idle filtering now uses three single-select pills (`All N`, `Active N`, `Idle N`) with amber `Active` and green `Idle` accents, keeping filter state clear while matching cockpit pill styling.
- Agent Cockpit: Compact mode now hides titlebar controls/chrome, stops re-forcing compact height after manual resize, and correctly accounts for grouped/divider layout units so rows are not hidden.
- Agent Cockpit: Grouped compact mode now adds bottom breathing room to keep the last row fully visible, and compact→full transitions now preserve current size instead of auto-resizing full mode.
- Agent Cockpit: Compact sizing now also updates correctly when live mode is disabled and keeps disabled callout content fully visible without clipping.
- Agent Cockpit: Row typography/colors for agent, project, and session names now match the main Session list style.
- Agent Cockpit: Agent-column labels now use plain text (no pill) and the same standard Codex/Claude accent colors used in the main Sessions list.
- Agent Cockpit: Removed persistent row-selection highlighting and removed up/down row navigation; row actions now use mouse click plus direct shortcut jumps (`Cmd+1...9`, `Cmd+0` for row 10).
- Agent Cockpit: Compact mode now keeps filter pills in the toolbar, removes the row keyboard-shortcut badge column, and trims compact sizing so no extra blank bands appear above the list or below the last visible row.
- Agent Cockpit: Compact mode now removes the remaining top titlebar gap so content starts directly with the compact toolbar row.
- Agent Cockpit: Removed the active/idle split divider from HUD rows and ensured compact/full HUD controls remain click-through responsive (no event interception by window configurator wiring).
- Preferences: Added a dedicated `Agent Cockpit` pane, moved Live Sessions + Cockpit settings there from `Advanced`, and added a `Show agent name in compact mode` toggle (default on).
- Preferences: Reordered the sidebar so `Agent Cockpit` now appears immediately after `General`.
- Agent Cockpit: Full mode now wraps long session names to two lines, while compact mode keeps names on one line with title text hidden and user-controlled sizing.
- Agent Cockpit: Replaced the `Agent Cockpit` window UI with the new HUD design (floating panel style, active/idle chips, inline filtering, grouped view, compact mode, pin mode, and row keyboard shortcuts), while keeping Legacy Cockpit available.
- Agent Cockpit: Window title now shows the currently displayed session count (`Agent Cockpit (N)`), removed the in-header `AGENT COCKPIT` label and footer status row, and removed the focused-window blue focus-ring box from the HUD list area.
- Menu/Cockpit windows: Removed `Legacy Cockpit` from the `View` menu, leaving `Agent Cockpit` as the only cockpit entry in the main menu.
- Menu/Cockpit windows: Removed `Legacy Cockpit` from the macOS `Window` menu while keeping the legacy scene code path in place.
- Agent Cockpit: This release keeps backend behavior parity with Legacy Cockpit and intentionally hides the branch column in all HUD modes.
- Menu/Cockpit windows: `View > Cockpit` was split into `Agent Cockpit` and `Legacy Cockpit`; both are single-instance windows.
- Shortcuts/filters: `⌘⌥⇧C` now opens `Agent Cockpit`, while `Legacy Cockpit` has no keyboard shortcut and defaults its filter mode to `Live`.
- Claude usage tracking: Hardened tmux probe cleanup by removing managed probe sockets at script exit and shifting orphan cleanup to socket-liveness classification, preventing stale-label churn and large CPU spikes when Claude tracking is enabled.
- Sessions (Gemini): Discovery now accepts named project folders under `~/.gemini/tmp` and continues to support both `chats/session-*.json` and direct `session-*.json` layouts, restoring pickup for newer Gemini CLI directory naming.
- Sessions (Agent Cockpit/Codex live): Pinned Agent Cockpit now refreshes faster while the app is backgrounded (3s cadence), so session rows and live status stay fresher without requiring app focus.
- Sessions (Agent Cockpit/Codex live): Foreground-return live-state probing now ramps in bounded batches and reuses a single iTerm session-list fetch across Codex+Claude, reducing short CPU spikes when switching back to Cockpit.
- Sessions (Agent Cockpit/Codex live-state): Codex ambiguous non-prompt iTerm tails now use log-write heuristic fallback instead of immediately forcing idle, reducing false-idle flips while preserving prompt-based idle detection.
- Sessions (Agent Cockpit/Codex live-state): Background iTerm probing is now deferred unless the app is foregrounded or a pinned cockpit is visible, reducing background probe load for non-pinned windows.
- Claude Usage: Multi-tier tracking with credential gating — OAuth endpoint (60s refresh), tmux fallback, optional claude.ai Web API mode with Full Disk Access guidance.
- Claude Usage: Unified refresh interval constant; hardened org UUID validation.
- Claude Usage: Safari cookie path corrected for sandboxed and legacy macOS layouts.
- Claude Usage: Guarded system preferences URL construction instead of force-unwrap.
- Claude Usage: Stale OAuth caches invalidated on 401; 429 rate-limit responses routed through Web API fallback.
- Preferences: Usage Tracking pane content now fits within available width — data source picker uses popup menu, segmented pickers fill space without overflow, strip toggles stacked vertically.
- Release hardening: Replaced force unwraps on calendar date arithmetic with safe fallbacks; wrapped diagnostic print statements in `#if DEBUG`; removed unused deprecated methods from WindowFocusCoordinator; converted open TODO markers to informational notes.
