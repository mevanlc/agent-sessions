## February 2026

- Usage tracking (menu bar): The menu bar quota tracker now shows a live updating spinner while Codex or Claude probes run, matching the in-app limit strip even when reset indicators are hidden.
- Sessions (Cockpit): Selected rows now use selection-aware foreground colors (instead of fixed agent/secondary colors), and Cockpit now uses the same selection accent/table style as the main Sessions list for improved readability.
- Sessions (Cockpit): Header filters now show only `Active` and `Live` (removed the extra `Cockpit`/`Show` labels), where `Live` includes both active and idle sessions; Cockpit window sizing now defaults to about 8 rows while remaining fully resizable with smoother horizontal behavior.
- Preferences (Advanced): Reordered sections so `Saved Sessions` appears above `Search`.
- Preferences (Advanced): Reordered the pane so `Git Context` is last, and renamed the section title to `Live Sessions + Cockpit BETA`.
- Preferences (Advanced): Added a single `Live Sessions + Cockpit (Beta)` control in Settings → Advanced and removed duplicate placement from Codex CLI settings.
- Sessions (Live/Cockpit): OpenCode live/open detection is deferred for this release (Codex + Claude only), and when the beta toggle is off, Cockpit/live controls stay visible but disabled with guidance text.
- Sessions (Unified): In the Sessions list `CLI Agent` column, active/open status dots now appear before the agent name, and double-clicking the `CLI Agent` cell now attempts to focus the active/open terminal with explicit alert feedback when focus is unavailable.
- Cockpit: `View > Cockpit` now reuses a single Cockpit window instead of opening multiple Cockpit windows.
- Sessions (Cockpit/Unified): Codex live session state now distinguishes `active` (working) vs `open` (idle); active rows use a pulsing dot and open rows use a solid dot.
- Sessions (Cockpit/Unified): Live-session support now includes Claude in addition to Codex; Cockpit now shows mixed live rows across these agents.
- Sessions (Unified): `Live sessions only` filtering and source-column live dots now apply to Codex and Claude sessions.
- Sessions (Cockpit/Unified): Cockpit now includes a `Both/Active/Open` live-session filter (default `Both`), and Unified live-only filtering now includes both active and open Codex sessions.
- Session view (Unified): Session list updates now keep prior rows/selection during transient empty refresh churn, preventing brief blank transcript placeholder flashes while indexing/search state settles.
- Sessions (Codex active indicators): iTerm live-state classification now ignores stale historical `Worked for` lines and only marks sessions active from near-tail live markers; transient iTerm tail-capture misses now stay `open` instead of flipping to false-active.
- Sessions (Codex active indicators): tty-only iTerm fallback presences are now reconciled into existing keyed rows by TTY, preventing duplicate live-session rows in Cockpit/Unified when registry/process and iTerm discovery overlap.
- Sessions (Cockpit): Cockpit now suppresses only low-confidence unresolved live placeholders (missing join keys and fallback identity signals like tty/pid/source/workspace), preventing ghost rows without hiding valid fallback-detected live sessions.
- Sessions (Cockpit): Unresolved `subagent` presences are now always hidden even when they carry inherited session/log join keys, removing lingering ghost live rows.
- Sessions (Claude/OpenCode live-state): iTerm tail probing now stays enabled for TTY-backed sessions even when terminal metadata is `tmux`/wrapper-based, improving active vs open classification.
- Sessions (Cockpit/Codex): Cockpit now suppresses unresolved Codex placeholders that do not join to indexed sessions, eliminating Cockpit-only ghost Codex rows.
- Sessions (Claude/OpenCode live-state): Generic tail classification now preserves active state when strong live markers are near-bottom (for example `Esc to interrupt`), while prompt-at-bottom clears stale/weak markers; weak busy markers are limited to near-bottom to reduce sticky active indicators.
- Sessions (Claude live-state): Claude now uses a dedicated tail classifier plus a wider `15s` mtime fallback window, reducing false-open states during active terminal output with sparse JSONL writes.
- Sessions (Claude live-state): Claude iTerm probing now uses `is processing` / `is at shell prompt` flags ahead of tail heuristics, and ANSI escape stripping in tail parsing now preserves active marker detection for styled terminal output.
- Sessions (Cockpit/Claude/OpenCode): Cockpit unresolved placeholders now require iTerm-backed identity or workspace joinability, suppressing `tmux`/TTY-only ghost rows for non-Codex providers.
- Sessions (Claude live-state): Fallback prompt parsing now treats zsh-style `%` prompt lines as idle while keeping raw percentage status lines (for example `78%`) non-prompt.
- Sessions (Claude live-state): iTerm probe conflict resolution now prioritizes `is processing` when both probe flags are true, and ambiguous no-flag tails are promoted to `active` only when prompt markers are absent near the bottom.
- Sessions (Cockpit/Claude/OpenCode): Unresolved non-Codex TTY-only rows now require explicit iTerm identity (or direct join/workspace matches) to remain visible, reducing persistent `tmux` ghost rows.
- Sessions (Claude live-state): Claude tail classification now treats generic lexical history as non-strong and keeps `active` reserved for strong live markers, reducing sticky false-active dots on idle prompts.
- Sessions (Claude live-state/Cockpit): TTY-backed Claude rows are now iTerm-attempt eligible even when `TERM_PROGRAM` reports wrappers like `tmux`; Claude tails now treat near-bottom weak busy markers as active when no prompt is present, and mtime fallback now uses registry source-file mtime when session log paths are missing.
- Sessions (Claude live-state): Claude iTerm probe metadata now uses a real tab-delimited processing/prompt header, and idle prompt detection now recognizes `❯ (No obvious next step)` lines to reduce false-active dots.
- Sessions (Cockpit/Claude/OpenCode): Cockpit now uses Unified’s one-to-one fallback mapping for unresolved live rows, so same-workspace concurrent sessions no longer collapse into one Cockpit row.
- Sessions (Cockpit): Same-session duplicate live rows now resolve by freshest presence telemetry instead of preferring stale `active` state, improving active/open correctness without needing Cockpit reopen.
- Sessions (Unified/Cockpit): Manual refresh now forces immediate live-session probing (not just index refresh), so active/open state transitions show up right away.
- Sessions (Unified): `CLI Agent` live-dot cells now rebind on active-membership updates, reducing delayed active/open dot repaint when list rows are otherwise unchanged.
- Sessions (Cockpit/Unified): Claude/OpenCode command-fallback matching now checks executable command positions only (argv0/wrapper target/shell `-c` head), preventing filename/argument mentions from producing phantom live rows.
- Sessions (Unified): Claude/OpenCode live fallback now maps multiple unresolved presences to multiple newest candidate sessions (workspace/source scoped), so concurrent same-repo live sessions remain visible in dots and `Live sessions only`.
- Sessions (Unified): Claude/OpenCode fallback ranking now excludes sessions already directly joined by session-id/log-path matches, so mixed direct+fallback live states no longer drop valid unresolved live rows.
- Sessions (Unified): Claude/OpenCode fallback presence caching now keys by source + session ID, preventing cross-provider ID collisions from mislabeling live dots or `Live sessions only` results.
- Sessions (Cockpit): Unresolved registry-only placeholders are now hidden unless they are focusable or workspace-joinable, preventing ghost sub-agent rows (for example `Active Codex CLI session` with empty project).
- Sessions (Unified/Cockpit): Unified session-list live-status dots now refresh on Codex active-membership updates even when `Active sessions only` is off, keeping active/open indicators in sync with Cockpit.
- Sessions (Cockpit/Unified): Cockpit now drops unresolved live placeholders that are neither focusable nor mappable to indexed workspace sessions, and unresolved rows are deduped by stable tty/workspace identity to reduce noisy duplicate rows.
- Sessions (Claude/Unified/Cockpit): Claude refresh now auto-escalates recent-scope drift to a full reconcile, and manual refreshes in both Unified and Cockpit now run Claude full reconcile, so newly opened Claude sessions are discovered more reliably in the main Sessions list.
- Sessions (Unified): Added a small active-status dot beside the agent name in the `CLI Agent` column for live Codex sessions.
- Sessions (Unified): Added a blue-dot, dot-only toolbar filter toggle before the agent toggles to show only active sessions.
- Cockpit/Sessions (Unified): Active Codex sessions are now joined by Codex internal `session_id` when log-path metadata is unavailable, restoring active indicators and Focus-in-iTerm2 availability.
- Sessions (Codex/Unified): Active-session matching now avoids event-derived ID fallback in row hot paths, and codex internal ID hint backfill now advances in rotating background batches for steady convergence.
- Sessions (Unified): Active-only mode now avoids duplicate per-row active checks when rendering source dots.
- Sessions (Cockpit): Removed `Copy Session ID` from Cockpit row menus; copy remains in the main Sessions list context menu.
- Claude usage probe cleanup: orphaned `as-cc-*` tmux label cleanup now runs in bounded batches with delayed continuation to reduce cleanup spikes, and excludes the currently active probe label from cleanup passes.
- Sessions (Unified): Removed the leading dot from agent pill toggles in the main toolbar.
- Sessions (Unified): Agent toolbar filter pills now show a clear enabled/disabled state in monochrome mode.
- Sessions (Unified): Fixed `active-only` list behavior so filtering applies immediately during running searches and selection is re-seated when a selected row is filtered out.
- Onboarding (2.12): Upgrades from `2.11.x` no longer auto-show onboarding; update onboarding remains enabled for upgrades from older versions.
- Claude usage probe: login-shell binary/path resolution now strips OSC escape noise (seen with iTerm2 shell integration), honors custom Claude binary overrides, and hardens tmux startup/trust-prompt handling to prevent false tmux-not-found and first-run probe failures.
- Release prep (2.12): Updated README and website download links/copy to point at `v2.12`, and aligned public highlights with transcript semantics, focused-monitor stability, and crash diagnostics improvements.
- Preferences (Unified Window): Moved `Columns` and `Filters` above `Rich Transcript` for a more logical settings flow.
- Preferences (OpenClaw): Moved `Include deleted OpenClaw sessions` out of Advanced and into the OpenClaw pane as a standalone checkbox.
- Transcript (Session view): Code-fence opening detection now requires line-start fences, so inline triple-backtick snippets no longer consume later fenced blocks or incorrectly style narrative text as code.
- Codex usage tracking: backward JSONL scans now continue parsing token-usage events while searching for the preferred `codex` rate-limit stream, preventing stale token fields when newer non-codex limit buckets are present.
- Sessions (OpenClaw): Tool-call/result parsing now accepts underscore/case variants (for example `tool_call` and `tool_result`), and `is_error` tool-result flags now map to error events.
- Transcript (Session view): Codex review-card parsing now skips malformed fenced JSON candidates and keeps scanning for later valid payloads, so one bad block no longer suppresses review summary rendering.
- Transcript (Session view): `path:line:column` linkification now preserves column targets reliably, preventing overlapping line-only matches from dropping column navigation.
- Transcript (Session view): Opening transcript file links in Cursor/VS Code is now asynchronous (with fallback preserved), so slow CLI startup no longer freezes transcript interaction.
- Transcript (Session view): Multiple Cursor/VS Code file-link opens now run concurrently, so slow/unavailable editor CLIs no longer delay later link opens behind earlier timeout waits.
- Session view (Unified): Transcript rendering now recognizes semantic assistant blocks (plans, code, diffs, and Codex review summaries) and displays them with distinct visual accents plus cleaner per-block grouping.
- Session view (Unified): Transcript toolbar semantic navigation now also detects code/diff content in tool-output blocks, and semantic totals remain accurate even when semantic filters are toggled.
- Session view (Unified/Terminal): Transcript toolbar now hides zero-result chips, falls back to compact marker+count chips when labels would overflow, and on very narrow widths moves extra chips under a chevron overflow menu.
- Session view (Unified/Terminal): Toolbar fit mode now refreshes correctly on width/layout switches, avoiding stale compact/overflow states after toggling split layouts.
- Session view (Unified): File references in transcripts are now clickable and can open in System default app, Cursor, or VS Code (with optional CLI override path), and Settings now includes Rich Transcript controls for review cards, linkification, and code/diff line numbers.
- Session view (Unified): Jump-to-latest arrow behavior is now consistent across Codex, Claude, Gemini, OpenCode, Copilot, Droid, and OpenClaw transcripts; it appears whenever the session is not at the end, including right after session switches.
- Session view (Unified): The jump-to-latest down-arrow now uses a smaller compact white circle and is shown immediately on session open whenever the transcript is not at the end.
- Session view (Unified): Clicking the jump-to-latest down-arrow now hides it immediately after auto-scroll reaches the end.
- Session view (Unified/Codex): Added a floating down-arrow in Session/Text modes that appears when new output arrives while scrolled up, so one click jumps to the latest output and resumes follow-tail behavior.
- Sessions (Unified): Key-window resign now preserves focused session state so transcript changes that happen while unfocused are visible immediately after returning to the window.
- Sessions (Unified): Focused active-session monitoring now routes through a per-source capability map (instead of Codex/Claude-only branches), preserving the one-focused-session model across all supported agent sources.
- Sessions (Gemini/OpenCode/Copilot/Droid/OpenClaw): Active-session reload paths now match Codex/Claude protections with same-session in-flight dedupe, unchanged-file fast-skip, and no monitor-triggered loading overlay flicker when transcript content is already loaded.
- Session view (Unified): Active session selection is now preserved across transient auto-refresh/search table churn, preventing transcript detachment to the empty placeholder while the session still exists.
- Session view (Unified): Selection routing is now canonical-ID driven (no table-selection bridge), so table churn cannot desync highlighted rows from the transcript pane and source routing stays stable while rows filter in/out.
- Session view (Unified): Transcript rebuilds now run from a stable session render key (`id`, `eventCount`, `events.count`, `fileSize`, `endTime`, `isFavorite`) to catch same-ID live updates and star toggles reliably.
- Search fields: `Esc` now clears the global search field and transcript Find field directly when that field is focused, avoiding the previous beep/no-op behavior.
- Transcript Find: pressing `Esc` with an empty Find field now closes the Find bar, restoring the advertised keyboard close behavior.
- Session view (Unified): programmatic table selection synchronization no longer trips manual-selection state, so auto-selection remains stable during internal list/selection coalescing.
- Session view (Unified): Removed the hidden-selection notice/actions (`Show in List` / `Keep Hidden`) from the transcript pane.
- Session view (Unified): Transcript tail-append now revalidates the previous tail event contents, forcing full rebuilds when live updates rewrite the prior tail event in place.
- Session view (Unified): Loaded-session transcript rendering now rebuilds on `eventCount`, file size, and end-time metadata changes, fixing stale transcript cases where `events.count` did not change.
- Session view (Unified/Session mode): Async transcript applies now enforce latest-generation gating, and Session mode tail updates now use deterministic signature/diff checks so rapid live refreshes do not flash stale or missing text.
- Session view (Unified/Session mode): Find navigation auto-scroll now triggers only on explicit navigation tokens (not passive refreshes), and canceled JSON builds now clear active loading state deterministically.
- Session view (Unified): Transcript rendering now keeps the last resolved session during transient reindex gaps, and coalesced programmatic table selection updates reduce reentrant selection churn that could blank the transcript pane until reselection.
- Session view (Unified): Transient table deselection during indexing/list refresh now preserves the active selection, and transcript host source fallback stays pinned to the last resolved selected session to prevent blank placeholder flashes.
- Sessions (Codex/Claude): Active-session transcripts no longer flash empty during periodic auto-refresh when a lightweight pass temporarily replaces fully parsed session data.
- Stability: Claude indexing now synchronizes refresh state/cache mutations and uses a thread-safe progress throttler, reducing intermittent background indexing crashes (`EXC_BAD_ACCESS`) observed during concurrent refresh activity.
- Session view (Unified): Transcript tail-append now populates the in-view build-key cache so quick mode switches during live updates no longer snap back to stale transcript text.
- Session view (Unified): Transcript tail-append now preserves render readiness state, so Unified Search auto-jump continues to work during append-only live updates.
- Session view (Unified): Transcript tail-append now requires the same render options as the current buffer, so toggles like “Skip preamble” force a full rebuild instead of appending with stale formatting.
- Session view (Unified): Manual refresh now consistently shows loading feedback for the selected session even when transcript text is already on screen.
- Session view (Unified/Terminal): Loading overlay now stays visible until the first terminal render completes, preventing brief blank panes and 0/0 navigation states on large sessions.
- Session view (Unified): Loading animation now remains visible while a newly selected session is still loading if the visible transcript buffer is from a different session, preventing stale-content flashes without loading feedback.
- Session view (Unified): Removed loading spinners from the transcript UI to avoid spinner-on-empty states while sessions load.
- Session view (Unified): Async transcript/JSON render bookkeeping now stores the source view mode, preventing transcript tail-append from running against buffers built for non-transcript modes after quick mode switches.
- Session view (Codex): Active transcript updates now append new tail content instead of replacing the full rendered transcript every refresh tick, removing periodic flicker and preserving reading/find context.
- Sessions (Codex): Focused background monitor refreshes no longer flash loading overlays when transcript content is already on screen.
- Usage tracking/menu bar: Codex and Claude now keep polling when usage is visible (including active in-app strip state), while inactive/background polling still requires that agent’s menu bar tracker; Codex background menu polling also now re-targets newer session JSONL files to avoid stalled reset updates.
- Claude Usage: tmux `/usage` probe waits now use a wider timeout window (based on script boot timeout) so slower successful probes are no longer terminated early with timeout failures.
- Sessions (Codex): Focused selected sessions now watch the active JSONL file and refresh transcript tails at an adaptive cadence (`~5s` when app active on AC, `~15s` otherwise), reducing stale tails while coding.
- Sessions (Claude): Focused selected sessions now watch the active JSONL file and force selected-session reloads on file changes, keeping live Claude transcript output attached during active refresh.
- Sessions (Unified/Codex/Claude): Focused-session refresh cadence is now source-aware and power/foreground-aware (Codex faster than Claude), and interval policy entries now exist for all agent sources so future focused-monitor expansion does not require monitor-loop rewrites.
- Sessions (Codex): `Refresh Sessions` now forces a full reload of the currently selected Codex session so the latest prompts and agent output appear immediately without changing selection.
- Sessions (Codex): Fixed a reload-dedupe race in focused-session monitoring so transcript tails no longer get stuck stale when files append during a full parse.
- Startup stability: Hardened launch-time analytics/onboarding observer lifecycle and startup controller ownership to reduce intermittent launch crashes.
- Analytics: Main-window close/reopen no longer drops analytics toggle observation; menu and shortcut toggles remain active throughout the app session.
- Sessions (Unified/Codex): Closing one window no longer disables shared active-session monitoring for other windows, closing the last Agent Sessions window now clears shared focus/activity state to stop background monitor loops, and manual refresh intent now persists through coalesced refreshes so selected Codex force reload is deterministic.
- Sessions (Unified): Focused-session monitoring now follows the key window per instance so closing a window does not leave stale focus when another main window remains open.
- Preferences/About: Added a Diagnostics section showing pending crash report count and last detected/email-draft timestamps, with `Email Crash Report`, `Export Report`, and `Clear Pending` actions plus direct support email link.
- Crash reporting: Added local crash queueing from macOS DiagnosticReports on launch; reports are shared only through explicit pre-filled email drafts (no automatic/background upload).
- Crash reporting UX: Removed the crash-capture toggle (capture is always on) and added a launch-time prompt with `Email Crash Report` or `Export + Open GitHub Issue` when new local crash reports are detected.
- Crash reporting reliability: Launch recovery now preserves pending reports when email/export does not complete, and deduplication now remembers all previously seen crash IDs to avoid repeated prompts for old reports.
- Crash reporting reliability: Detection now scans the full crash-report lookback window even in noisy DiagnosticReports directories, and seen-ID history now trims by recency to avoid re-queuing recently handled crashes when capped.
- Crash reporting reliability: Crash recovery now uses a single pending report (newest-first), avoiding queue drops from sharing one report out of a multi-entry queue.
- Crash reporting reliability: Seen-ID persistence now happens only when pending reports are handled/cleared, and crash-report envelopes now keep version/build from the source report for accurate post-upgrade triage.
- Crash reporting reliability: Clearing pending reports now records all cleared crash IDs as seen, preventing repeat prompts from legacy or multi-entry pending queues.
- Crash reporting reliability: Seen IDs are now persisted only after pending clear succeeds, so failed clear attempts do not hide still-pending crash reports.
- Hotfix: Removed launch-time `NSApp.isActive` reads from Codex/Claude usage singleton initialization and moved app-active synchronization to post-window startup to prevent startup SIGTRAP crashes.
- Major update: Added OpenClaw sessions (including optional visibility for deleted OpenClaw transcripts).
- Major update: Expanded Images workflows across Session view inline thumbnails and Image Browser.
- Major bug fix: Search is more complete for sessions that previously stayed partially indexed.
- Major bug fix: Reduced Energy Warning spikes with activation-driven refresh and power-aware background behavior.
- Cockpit: Added a Cockpit window for active Codex sessions with iTerm2 focus, plus ACTIVE badges in the Unified sessions list.
- Cockpit: Fixed mixed registry/probe active-session detection so all active Codex sessions stay visible, and limited Focus enablement to iTerm-targetable sessions.
- Image Browser: Now supports Claude Code sessions.
- Image Browser: Now supports OpenCode sessions.
- Image Browser: Now supports Gemini and Copilot sessions.
- Image Browser: Added Project and Agent filters, a larger preview pane, and user prompt context for each image when available.
- Image Browser: Caches image indexes and thumbnails for faster open and to avoid reprocessing previously seen sessions.
- Image Browser: Added thumbnail right-click actions and a preview Actions menu for common image actions.
- Session view: Inline image thumbnail click hit-testing is more reliable after scrolling or transcript updates.
- Session view: Inline image thumbnails now open the Image Browser on single click and include an “Open in Image Browser” context menu action.
- Session view: Shift-clicking an inline image no longer opens the Image Browser, preserving selection behavior.
- Session view: Tool outputs (including OpenClaw `{text,type}` block arrays) now preserve formatting and render as plain text.
- Session view: Real user prompts now use the same narrative font as other blocks (no custom user-only typeface).
- Session view: User prompt blocks now remove side accent strips, keep inverted contrast in dark mode, and use a dark gray (`white: 0.20`) bubble with white text in light mode.
- Session view: Removed the synthetic “Conversation starts here” divider line for all agents while preserving first-real-prompt auto-jump when preamble skipping is enabled.
- Session view: The Images toolbar pill now shows only the icon and count (no “Images” text label).
- Session view: The Images toolbar pill now always appears and stays disabled until images are detected for the selected session; once available it enables with X/Y count and navigation.
- Session view: Inline image thumbnails now support Gemini and Copilot sessions.
- Sessions: Added OpenClaw (clawdbot) session support when the OpenClaw/clawdbot CLI is installed; deleted sessions can be shown via an Advanced toggle.
- Image Browser: OpenClaw images require explicitly selecting OpenClaw in the Agent filter; “All Agents” is now “All Coding Agents”.
- Session view: Inline image thumbnails now support OpenClaw sessions, and OpenClaw project metadata stays consistent after opening a session.
- OpenClaw: Session view hides the verbose media-attachment hint text, formats `exec` outputs like shell output (with exit codes), and Image Browser rebuilds stale indexes so images appear.
- Performance: Replaced always-on warmup/archive timers with app-activation and event-driven refresh paths to reduce idle battery/CPU wakeups.
- Usage tracking: Claude `/usage` background probing is now AC-only; battery and Low Power Mode use manual refresh, while Codex polling remains near-live with cheaper unchanged-file ticks.
- Sessions/Search: While typing in global Search, results now stay on fast indexed paths; pressing Return triggers deep/unindexed scan to avoid long typing-time CPU bursts.
- Sessions/Search: Global Search now avoids generating transcripts on-demand and instead uses cached transcripts when available and raw event fields otherwise to reduce sustained CPU bursts after searches.
- Sessions/Indexing: Codex and Claude transcript prewarm tasks now cancel older runs and cap per-refresh work to reduce sustained post-refresh energy spikes.
- Sessions/Indexing: Deferred non-manual refresh work while the app is inactive; it resumes on foreground activation to reduce background energy warnings.
- Codex Usage: Automatic tmux `/status` fallback probes now run less aggressively (stricter stale/no-recent checks plus longer cooldown) and use lighter log-tail scans.
- Sessions/Search: Codex and Claude refresh now process changed files incrementally in small batches with capped concurrency and low-priority background deferral when inactive or on battery.
- Sessions/Search: Gemini, Copilot, Droid, and OpenClaw now use the same power-aware idle indexing profile behavior (reduced worker pressure and deferred non-critical work when inactive or on battery).
- Preferences: Added an OpenClaw pane after the other agent panes with matching Binary Source and Sessions Directory controls.
- Preferences: Added a divider between Droid and OpenClaw in the sidebar, and added `Update...` actions on each agent pane to detect package manager ownership, check for newer versions, and run updates after confirmation.
- Preferences: Hardened agent update detection by resolving manager binaries from common filesystem paths and inferring npm package IDs from the resolved CLI binary (including OpenClaw/clawdbot).
- Preferences/Updates: Added an `Auto-Update` checkbox beside `Check for Updates...` in Settings → About, and defaulted Sparkle auto-update to enabled with opt-out support.
- Onboarding: Added a first-slide feedback card under the session counters with the public Google Form link and local-only/no-telemetry messaging so users know this is the feedback channel.
- Onboarding: Fixed the final-slide weekly chart overlap, split usage limit tracking into separate Claude/Codex sections, and resized/refined layouts so onboarding slides no longer require scrolling.
- Sessions (Cockpit): Active-session registry override values now expand `~` before shell injection, so tilde-based overrides are consistently discoverable by active-session discovery and focus logic.
- Sessions (Cockpit): Process fallback probing now honors the existing probe cooldown even when registry snapshots are empty, preventing per-2-second `ps/lsof` churn.
- Sessions (Codex active indicators): Path normalization for active-session joins now uses cached canonical (symlink-aware) normalization so equivalent roots (for example `/var/...` and `/private/var/...`) still match, fallback process probes now run as periodic sweeps (`30s` foreground / `120s` background) when registry data is present, and active polling/probing now backs off while the app is inactive.
- Sessions (Codex active indicators): Unified/Cockpit visibility for active polling is now tracked per window instance, so closing one of multiple open windows no longer throttles refresh cadence for still-visible windows.
- Sessions (Codex active indicators): Disabling and re-enabling active detection now preserves visible-window registrations, so active refresh cadence immediately returns to foreground behavior in already-open Unified/Cockpit windows.
